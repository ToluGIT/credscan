name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"

      - name: Run tests with coverage
        run: |
          pytest tests/ --cov=credscan --cov-report=xml --cov-report=term-missing -v

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        if: matrix.python-version == '3.12'
        with:
          files: ./coverage.xml
          fail_ci_if_error: false

  benchmark:
    name: Detection Benchmark
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install credscan
        run: pip install -e .

      - name: Run benchmark (fail if F1 regresses)
        run: |
          PYTHONPATH=src python benchmarks/run.py --fail-under-f1 0.90

  self-scan:
    name: Self-Scan (dogfood)
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install credscan
        run: pip install -e .

      - name: Scan this repository for leaked credentials
        run: |
          # Exclude directories that contain synthetic test secrets by design.
          credscan --path . \
            --exclude ".git/,.github/,tests/,benchmarks/,demo/,*.pyc" \
            --output json \
            --output-dir /tmp/scan-results \
            --min-confidence 0.5 || true

      - name: Check scan results
        run: |
          if ls /tmp/scan-results/*.json 2>/dev/null; then
            FINDINGS=$(python3 -c "
          import json, glob, sys
          files = glob.glob('/tmp/scan-results/*.json')
          if not files:
              print('0')
              sys.exit(0)
          data = json.load(open(files[0]))
          findings = [f for f in data.get('findings', []) if f.get('severity') in ('critical', 'high')]
          print(len(findings))
          ")
            echo "High/critical findings: $FINDINGS"
            if [ "$FINDINGS" -gt "0" ]; then
              echo "::warning::$FINDINGS high/critical credential findings in repository"
            fi
          fi

  lint:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install lint tools
        # Pinned so CI and the local dev extra format identically; an
        # unpinned upgrade should not silently fail an unrelated PR.
        run: pip install black==26.5.1 isort==8.0.1

      - name: Check formatting (black)
        run: black --check src/

      - name: Check imports (isort)
        run: isort --check-only src/
